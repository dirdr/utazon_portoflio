# OS / editor
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
*.sw?

# Agent tooling
.claude

# Environment variables — each app keeps its own .env
.env

# Per-app build output and dependencies are ignored in apps/*/.gitignore
